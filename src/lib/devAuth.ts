// Stubbed user identity for the scaffold step. Auth (Convex Auth or
// Clerk) is its own future step. When that lands, every reference to
// DEV_USER_ID and every v.string() userId column gets swapped for the
// authenticated identity.
export const DEV_USER_ID = "dev-user-1";
